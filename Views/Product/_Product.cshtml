@model StoreKom.Models.Product

<div class="product">
    <h2>@Model.Name</h2>
    <p>@Model.Description</p>
    <div class="price">@Model.Price</div>
    <p>@Html.ActionLink("Edytuj", "Edit", new { id = Model.Id })</p>
</div>
